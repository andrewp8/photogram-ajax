var existingRequest = $("#unfollow_request_<%= @follow_request.recipient_id %>");
var updatedRequest = $("<%= j(render "form", follow_request: @follow_request  ) %>")
existingRequest.replaceWith(updatedRequest)
